# Frontend Component Releases — 2026

**Refs:** → [000_index](../000_index.md) · [Deployment Strategy](../../sdd/infra/01_deployment.md) · [Git and Release Strategy](../../sdd/workflow/02_git_and_release_strategy.md)

Per `sdd-framework/08_release_process.md`: one entry per actual deployment, tag-anchored, no restated diffs — see `git log`/`git show` at the referenced tag for the full change detail.

## `frontend-v0.1.0` — 2026-07-17

- **Tag:** `frontend-v0.1.0`
- **Commit:** `a3494f9`
- **Deployed to:** [https://hypora-2026.vercel.app](https://hypora-2026.vercel.app) (production), Vercel project `ttuis-projects/app`
- **Deployment target:** the single bundled Vercel project (Landing + Workspace), per [Deployment Strategy](../../sdd/infra/01_deployment.md) / [ADR-0003](../../sdd/architecture/decisions/ADR-0003-single-v1-deployment-target.md)

First real production deployment of this component — the trigger for creating `release/` itself, per `sdd-framework/08_release_process.md`'s "only when something is actually deployed" rule. Pre-migration history (every deploy before this tag, including several earlier Production-target Vercel deployments made without a corresponding git tag) predates the tagging convention and is not retroactively assigned a version, per the framework's "Migrating an Existing Changelog" guidance — this tag anchors the current/baseline state going forward, not a reconstructed history.

Landing's redesign (target information architecture, three narrative concepts, promoted `sdd/landing/` specification, Vision section replacing Roadmap, header layout fix) shipped as part of this release — see `sdd/landing/` for the specification and `git log a3494f9` for the full commit history leading to it.
